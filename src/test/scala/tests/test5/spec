
monitor M2 {
  event command(cmd,kind), succeed(cmd)

  always {
    command(cmd : x, kind : "FSW") => Succeed(c : x)
  }

  hot Succeed(c) {
    succeed(cmd : c) => ok
    command(cmd : c) => error
  }
}